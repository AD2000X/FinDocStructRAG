# Local development: pure-CPU logic and pure-API LLM calls (no GPU).
pandas
numpy
beautifulsoup4
lxml
# Used by src/visualisation.py to draw the Phase 1A deliverable figures (CPU only).
Pillow
# Answer-generation LLM SDK: OpenRouter is OpenAI-compatible (single provider, see
# src/llm_client.py).
openai
